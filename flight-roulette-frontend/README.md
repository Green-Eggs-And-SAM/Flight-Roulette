1st install and run the backend (see flight roulette backend readme.md)

Dependencies:
-axios
-react-router-dom
-sass

use 'npm i <dependency>' to install the necessary dependencies

use npm start to start the front end.
